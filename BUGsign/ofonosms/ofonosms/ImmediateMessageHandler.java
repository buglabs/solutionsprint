package ofonosms;

import org.freedesktop.dbus.DBusSigHandler;
import org.ofono.MessageManager;

public class ImmediateMessageHandler implements DBusSigHandler<MessageManager.ImmediateMessage> {
	public void handle(final MessageManager.ImmediateMessage im) {
		
	}
}
