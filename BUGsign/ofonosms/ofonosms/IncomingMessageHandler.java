package ofonosms;

import org.freedesktop.dbus.DBusSigHandler;
import org.ofono.MessageManager;

public class IncomingMessageHandler implements DBusSigHandler<MessageManager.IncomingMessage> {
	public void handle(final MessageManager.IncomingMessage im) {
		
	}
}
